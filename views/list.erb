<h1><%= @list.name() %></h1>
<% if @list.tasks().any?() %>
  <h3>Here are all the tasks in this lists:</h3>
  <ul>
    <% @list.tasks().each() do |task| %>
    <li><%= task.description() %> - due <%= task.due() %> [<a href="/delete_task/<%= task.id()%>">x</a>]</li>
    <% end %>
  </ul>
<% else %>
  <p>There are no tasks on this list!</p>


<% end %>
